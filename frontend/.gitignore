# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
# Committed on purpose: holds only VITE_API_URL, a public build-time value.
# Vite client vars are baked into the browser bundle, so this can never hold
# a secret anyway — and the production build needs it to reach the backend.
!.env.production
e2e-artifacts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
